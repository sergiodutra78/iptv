diff --git a/node_modules/cordova-plugin-streaming-media/src/android/com/hutchind/cordova/plugins/streamingmedia/SimpleVideoStream.java b/node_modules/cordova-plugin-streaming-media/src/android/com/hutchind/cordova/plugins/streamingmedia/SimpleVideoStream.java
index 463a747..84653ca 100644
--- a/node_modules/cordova-plugin-streaming-media/src/android/com/hutchind/cordova/plugins/streamingmedia/SimpleVideoStream.java
+++ b/node_modules/cordova-plugin-streaming-media/src/android/com/hutchind/cordova/plugins/streamingmedia/SimpleVideoStream.java
@@ -8,14 +8,19 @@ import android.media.MediaPlayer;
 import android.widget.MediaController;
 import android.content.Intent;
 import android.content.pm.ActivityInfo;
+import android.os.Build;
 import android.view.MotionEvent;
 import android.net.Uri;
 import android.os.Bundle;
 import android.util.Log;
+import android.util.TypedValue;
 import android.view.Display;
 import android.view.View;
 import android.view.Window;
+import android.view.WindowInsets;
+import android.view.WindowInsetsController;
 import android.view.WindowManager;
+import android.widget.Button;
 import android.widget.MediaController;
 import android.widget.ProgressBar;
 import android.widget.RelativeLayout;
@@ -29,6 +34,7 @@ MediaPlayer.OnErrorListener, MediaPlayer.OnBufferingUpdateListener {
 	private MediaPlayer mMediaPlayer = null;
 	private MediaController mMediaController = null;
 	private ProgressBar mProgressBar = null;
+	private Button mCloseButton = null;
 	private String mVideoUrl;
 	private Boolean mShouldAutoClose = true;
 	private boolean mControls;
@@ -63,13 +69,46 @@ MediaPlayer.OnErrorListener, MediaPlayer.OnBufferingUpdateListener {
 		relLayout.addView(mProgressBar);
 		mProgressBar.bringToFront();
 
+		// Close button, so there is a visible way out besides the back gesture
+		mCloseButton = new Button(this);
+		mCloseButton.setText("✕");
+		mCloseButton.setTextColor(Color.WHITE);
+		mCloseButton.setTextSize(TypedValue.COMPLEX_UNIT_SP, 16);
+		mCloseButton.setAllCaps(false);
+		mCloseButton.setPadding(0, 0, 0, 0);
+		mCloseButton.setBackgroundColor(Color.argb(150, 0, 0, 0));
+		RelativeLayout.LayoutParams closeParams = new RelativeLayout.LayoutParams(dpToPx(44), dpToPx(44));
+		closeParams.addRule(RelativeLayout.ALIGN_PARENT_TOP);
+		closeParams.addRule(RelativeLayout.ALIGN_PARENT_RIGHT);
+		closeParams.setMargins(0, dpToPx(20), dpToPx(20), 0);
+		mCloseButton.setLayoutParams(closeParams);
+		mCloseButton.setOnClickListener(new View.OnClickListener() {
+			@Override
+			public void onClick(View v) {
+				wrapItUp(RESULT_OK, null);
+			}
+		});
+		relLayout.addView(mCloseButton);
+		mCloseButton.bringToFront();
+
 		setOrientation(b.getString("orientation"));
 
 		setContentView(relLayout, relLayoutParam);
 
+		// Needs the decor view, so only after setContentView
+		hideSystemBars();
+
 		play();
 	}
 
+	@Override
+	public void onWindowFocusChanged(boolean hasFocus) {
+		super.onWindowFocusChanged(hasFocus);
+		if (hasFocus) {
+			hideSystemBars();
+		}
+	}
+
 	private void play() {
 		mProgressBar.setVisibility(View.VISIBLE);
 		Uri videoUri = Uri.parse(mVideoUrl);
@@ -90,6 +129,31 @@ MediaPlayer.OnErrorListener, MediaPlayer.OnBufferingUpdateListener {
 		}
 	}
 
+	private int dpToPx(int dp) {
+		return (int) TypedValue.applyDimension(TypedValue.COMPLEX_UNIT_DIP, dp, getResources().getDisplayMetrics());
+	}
+
+	// setSystemUiVisibility is ignored for apps targeting SDK 35+, so use the
+	// WindowInsetsController API to get a real immersive fullscreen video.
+	private void hideSystemBars() {
+		if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.R) {
+			getWindow().setDecorFitsSystemWindows(false);
+			WindowInsetsController controller = getWindow().getInsetsController();
+			if (controller != null) {
+				controller.hide(WindowInsets.Type.systemBars());
+				controller.setSystemBarsBehavior(WindowInsetsController.BEHAVIOR_SHOW_TRANSIENT_BARS_BY_SWIPE);
+			}
+		} else {
+			getWindow().getDecorView().setSystemUiVisibility(
+				View.SYSTEM_UI_FLAG_IMMERSIVE_STICKY
+				| View.SYSTEM_UI_FLAG_FULLSCREEN
+				| View.SYSTEM_UI_FLAG_HIDE_NAVIGATION
+				| View.SYSTEM_UI_FLAG_LAYOUT_STABLE
+				| View.SYSTEM_UI_FLAG_LAYOUT_FULLSCREEN
+				| View.SYSTEM_UI_FLAG_LAYOUT_HIDE_NAVIGATION);
+		}
+	}
+
 	private void setOrientation(String orientation) {
 		if ("landscape".equals(orientation)) {
 			this.setRequestedOrientation(ActivityInfo.SCREEN_ORIENTATION_LANDSCAPE);
